# 隐私与第三方服务说明

## 数据流

启用本插件后：

1. 用户浏览器加载极验第四代 Web SDK。
2. 浏览器与极验服务交互完成人机验证。
3. 验证成功后，浏览器把 `lot_number`、`captcha_output`、`pass_token` 和 `gen_time` 交给 V10 业务接口。
4. 插件服务器把上述字段和服务端生成的 `sign_token` 提交给极验二次校验。

Captcha Key 不下发到浏览器。本项目不会主动把登录密码、短信验证码、邮箱验证码或完整用户资料发送给极验。

## 部署者责任

站点运营者应：

- 阅读并接受极验当前服务协议、隐私政策和合规指引；
- 在 `https://www.example.com/privacy` 等自身隐私政策页面披露第三方验证码处理；
- 根据适用法律确定合法性基础、告知方式、保存期限和用户权利处理流程；
- 评估极验 SDK 可能收集的设备、网络和行为信息；
- 仅在必要的高风险场景启用验证；
- 限制 V10 后台、数据库、缓存和日志访问权限。

## 日志

插件只记录经过清理的错误类别、HTTP 状态、cURL 错误码和验证流水号的短摘要，不记录：

- Captcha Key；
- 完整验证结果；
- 本地一次性 Token；
- 用户密码；
- 手机或邮箱验证码。

运营方仍应检查 Web 服务器、反向代理、APM 和错误收集系统，避免它们记录完整请求体。
