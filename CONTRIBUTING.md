# 贡献指南

感谢参与本项目。提交代码前请遵守以下边界：

1. 不修改 ZJMF-CBAP Core；本项目只维护验证码插件。
2. 保持稳定身份 `captcha / GeetestCaptcha / geetest_captcha`，不得通过改名替代正常升级。
3. 不加入绕过、人工作答、自动识别或供应商异常放行逻辑。
4. 不提交真实 Captcha ID/Key、用户数据、服务器资料、签名私钥或生产日志。
5. 新配置必须形成真实、可验证的产品差异；固定安全默认值不应变成可随意放宽的开关。
6. `upgrade()` 必须幂等，升级不得要求用户卸载重装。
7. 用户界面错误使用可理解的业务语言，不向普通用户暴露接口、缓存、堆栈或凭据细节。

## 本地检查

```bash
php scripts/check.php
git diff --check
```

`scripts/check.php` 会执行 PHP 语法、运行契约、敏感信息扫描、安装包结构检查和可复现构建检查。

Pull Request 应说明：问题、兼容影响、安全影响、测试证据和升级路径。改变运行文件时必须更新 `CHANGELOG.md`，并在目标 V10 环境重新验证普通首次安装和原位升级。
