# V10 兼容性

## 插件信息

| 项目 | 值 |
| --- | --- |
| 插件类型 | 图形验证码（Captcha） |
| 插件目录 | `geetest_captcha` |
| 插件名称 | `GeetestCaptcha` |
| 当前版本 | `1.3.0` |
| PHP 要求 | PHP 7.3 或更高版本 |

## 已验证环境

| ZJMF-CBAP 版本 | 兼容状态 |
| --- | --- |
| 10.7.2 | 已验证 |
| 10.4.6 | 兼容基线 |

其他 V10 版本如果保持相同的图形验证码插件接口，通常也可以使用。正式部署前建议在测试环境验证插件导入、配置保存和实际业务场景。

## 兼容说明

- 插件使用 V10 原生图形验证码接口，不修改系统 Core。
- 可保护的注册、登录、重置密码、后台登录和联系方式操作由当前 V10 版本的系统安全设置决定。
- 多节点部署必须使用共享缓存，确保验证码请求和业务提交能够读取同一个 Token 状态。
- GT3 与 GT4 凭据不能混用；本插件仅支持 GT4 Captcha ID 和 Captcha Key。
- 插件只负责人机验证。手机号、邮箱、用户身份和短信/邮件验证码用途仍由 V10 业务接口校验。
